rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Open rules for testing only. Do NOT use in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
